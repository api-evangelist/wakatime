naftiko: 1.0.0-alpha2
info:
  label: WakaTime API — Status Bar
  description: 'WakaTime API — Status Bar. 1 operations. Lead operation: Get Status Bar. Self-contained Naftiko capability covering one WakaTime business surface.'
  tags:
  - WakaTime
  - Developer Productivity
  - Status Bar
  created: '2026-05-30'
  modified: '2026-05-30'
binds:
- namespace: env
  keys:
    WAKATIME_API_KEY: WAKATIME_API_KEY
capability:
  consumes:
  - type: http
    namespace: wakatime-api-v1-status-bar
    baseUri: https://wakatime.com/api/v1
    description: WakaTime API — Status Bar business capability. Self-contained, no shared references.
    authentication:
      type: basic
      username: '{{env.WAKATIME_API_KEY}}'
      password: ''
    resources:
    - name: users-current-status-bar
      path: /users/current/status_bar
      operations:
      - name: getStatusBar
        method: GET
        description: Get Status Bar
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
  exposes:
  - type: rest
    namespace: wakatime-api-v1-status-bar-rest
    port: 8080
    description: REST adapter for WakaTime API — Status Bar. One Spectral-compliant resource per consumed operation, prefixed with /v1.
    resources:
    - path: /v1/users/current/status-bar
      name: users-current-status-bar
      description: REST surface for /users/current/status_bar.
      operations:
      - method: GET
        name: getStatusBar
        description: Get Status Bar
        call: wakatime-api-v1-status-bar.getStatusBar
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: wakatime-api-v1-status-bar-mcp
    port: 9090
    transport: http
    description: MCP adapter for WakaTime API — Status Bar. One tool per consumed operation, routed inline through this capability's consumes block.
    tools:
    - name: get-status-bar
      description: Get Status Bar
      hints:
        destructive: false
        idempotent: true
        readOnly: true
      call: wakatime-api-v1-status-bar.getStatusBar
      outputParameters:
      - type: object
        mapping: $.
